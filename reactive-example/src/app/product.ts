export class Product {
    // tslint:disable-next-line: max-line-length
    constructor(public productId: number, public productName: string, public productDescription: string, public productImage: string, public productPrice: number, public productQuantity: number) {}
}
